<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Mm.NoActionbar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/main</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Mm.Splash" parent="Theme.SplashScreen">
<!--        &lt;!&ndash; 배경 색 &ndash;&gt;-->
<!--        <item name="windowSplashScreenBackground">@color/white</item>-->
        <!-- 화면 중앙 아이콘 -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon_foreground</item>
        <!-- 스플래시 지속 시간 -->
        <item name="windowSplashScreenAnimationDuration">1000</item>
        <!-- 스플래시 후 보일 화면 테마 -->
        <item name="postSplashScreenTheme">@style/Theme.Mm.NoActionbar</item>

        <!-- 필요시 다크모드 같은 상황 고려. Status bar and Nav bar configs -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:navigationBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>